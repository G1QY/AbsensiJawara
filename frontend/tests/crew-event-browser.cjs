const { chromium } = require(process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES + '/playwright');
const http=require('node:http'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const root=path.resolve('dist');
const server=http.createServer((req,res)=>{const p=new URL(req.url,'http://localhost').pathname;const file=path.join(root,p==='/'?'index.html':p);fs.readFile(file,(e,data)=>{if(e){res.writeHead(404);return res.end();}res.setHeader('Content-Type',file.endsWith('.js')?'application/javascript':file.endsWith('.css')?'text/css':file.endsWith('.jpg')?'image/jpeg':'text/html');res.end(data);});});
(async()=>{const binary=(await import(process.env.CHROMIUM_MODULE)).default;await new Promise(r=>server.listen(0,'127.0.0.1',r));const origin='http://127.0.0.1:'+server.address().port;let browser;
try{browser=await chromium.launch({headless:true,executablePath:process.env.CHROMIUM_EXECUTABLE||await binary.executablePath(),args:binary.args});const page=await browser.newPage({viewport:{width:1365,height:900}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
const workflow={event_id:'event-1',data:{inventory_before_items:[{nama:'Kamera',jumlah:2,kategori:'Electronic',kondisi:'Baik'}],inventory_after_items:[{nama:'Kamera',jumlah:2,kondisi:'Baik'}],transportasi_pergi_nominal:50000,tes_print_jumlah:2,omset_nominal:500000},current_step:5,max_reached:5,updated_at:'2026-09-01T03:00:00Z'};
const workspace={crew:{id:'crew-1',employee_code:'BDG-EVT-1',base_salary:2000000,user:{full_name:'Crew Bandung'}},assignments:[{id:'assignment-1',position:'PIC',status:'ACTIVE',event:{id:'event-1',event_code:'BDG-SMA1',event_name:'Event SMA 1 Bandung',client_name:'SMA 1',event_date:'2026-09-01',status:'ONGOING',branch:{name:'Bandung'},event_locations:[{address:'SMA 1 Bandung'}],pic:{user:{full_name:'Crew Bandung'}}},event_schedules:[{id:'schedule-1',schedule_date:'2026-09-01',start_time:'08:00:00',end_time:'17:00:00',status:'ACTIVE'}],workflow,team:['Crew Bandung','Crew Dua'],attendance:[]}]};
await page.route('**/*',async route=>{const url=new URL(route.request().url());if(url.origin===origin)return route.continue();if(!url.pathname.startsWith('/api/'))return route.abort();let data={};if(url.pathname==='/api/auth/login')data={token:'fixture',refreshToken:'refresh',user:{id:'user-1',full_name:'Crew Bandung',email:'crew@test.local'},role:'CREW_EVENT'};else if(url.pathname==='/api/users/me')data={id:'user-1',full_name:'Crew Bandung',email:'crew@test.local'};else if(url.pathname==='/api/crew-event/workspace')data=workspace;else if(url.pathname==='/api/crew-event/events/event-1/workflow')data=workflow;else if(url.pathname==='/api/notifications')data=[];else if(url.pathname==='/api/auth/logout')data={message:'ok'};return route.fulfill({status:200,contentType:'application/json',body:JSON.stringify(data)});});
await page.goto(origin);await page.locator('#email').fill('crew@test.local');await page.locator('#password').fill('password-uji');await page.getByRole('button',{name:'Log in',exact:true}).click();await page.getByText('Event SMA 1 Bandung',{exact:true}).waitFor();
const side=page.locator('aside:visible');for(const label of ['Dashboard','Event Saya','Absensi','Inventory','Operasional','Riwayat','Payroll'])assert.equal(await side.getByRole('button',{name:label,exact:true}).count(),1,label);assert.equal(await side.getByRole('button',{name:'Kertas',exact:true}).count(),0);const bg=await side.evaluate(el=>getComputedStyle(el).backgroundColor);assert.notEqual(bg,'rgb(15, 23, 42)');
await side.getByRole('button',{name:'Event Saya',exact:true}).click();await page.getByText('Event muncul setelah admin menugaskan akun Anda melalui Kelola Event.').waitFor();await page.getByRole('button',{name:'Lanjutkan',exact:true}).click();await page.getByText('Progress Workflow',{exact:true}).waitFor();assert.equal(await page.getByText('BDG-SMA1 • Event SMA 1 Bandung',{exact:true}).count(),1);
await side.getByRole('button',{name:'Inventory',exact:true}).click();await page.getByText('Perbandingan Inventory Before vs After',{exact:true}).waitFor();await side.getByRole('button',{name:'Operasional',exact:true}).click();await page.getByText('Total Omset',{exact:true}).waitFor();assert.deepEqual(errors,[]);
}finally{if(browser)await browser.close();await new Promise(r=>server.close(r));}})().catch(error=>{console.error(error);process.exitCode=1;});
