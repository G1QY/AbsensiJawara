import ExportButtons from '../../components/ui/ExportButtons';
import {payroll,rupiah,useStoreWorkspace} from './crewStoreWorkspace';

export default function CrewStorePayroll(){const{data,loading,error}=useStoreWorkspace();if(loading)return <div className="p-6">Memuat payroll…</div>;if(error)return <div className="p-6"><p className="rounded-2xl border border-red-200 bg-red-50 p-4 text-red-700">{error}</p></div>;const p=payroll(data),period=new Date().toLocaleDateString('id-ID',{month:'long',year:'numeric',timeZone:'Asia/Jakarta'});return <div className="p-4 sm:p-6 space-y-4"><section className="rounded-2xl bg-gradient-to-r from-emerald-600 to-teal-600 p-5 text-white flex flex-wrap justify-between gap-4"><div><p className="text-sm text-emerald-100">Payroll {period}</p><h1 className="text-2xl font-bold mt-1">{data?.crew.user?.full_name||'Crew Store'}</h1><p className="text-sm text-emerald-100 mt-1">{data?.assignment?.store?.name||'Crew Store'}</p></div><ExportButtons filename={`Payroll-Crew-Store-${period}`} title="Estimasi Payroll Crew Store" subtitle={`${data?.crew.user?.full_name||'Crew'} • ${period}`} headers={['Gaji Pokok','Hari Terjadwal','Hari Hadir','Hari Absen','Potongan Absen','Menit Telat','Potongan Telat','Jam Lembur Disetujui','Bonus','Estimasi']} rows={[[p.base,p.scheduledDays,p.present,p.absentDays,p.absenceDeduction,p.lateMinutes,p.lateDeduction,p.overtimeHours,p.bonus,p.total]]}/></section><section className="bg-white border border-slate-200 rounded-2xl p-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-slate-500">Estimasi diterima</p><strong className="block text-4xl text-emerald-600 mt-2">{rupiah(p.total)}</strong></div><div className="text-right"><p className="text-sm font-semibold text-slate-800">{p.present} hadir dari {p.scheduledDays} hari kerja</p><p className="text-xs text-slate-500 mt-1">{period}</p></div></div></section><div className="grid sm:grid-cols-2 xl:grid-cols-3 gap-4"><Box label="Gaji Pokok" value={rupiah(p.base)}/><Box label={`Potongan Absen, ${p.absentDays} hari`} value={`-${rupiah(p.absenceDeduction)}`} tone="red"/><Box label="Potongan Keterlambatan" value={`-${rupiah(p.lateDeduction)}`} tone="red"/><Box label="Bonus Lembur" value={`+${rupiah(p.bonus)}`} tone="green"/><Box label="Lembur Disetujui" value={`${p.overtimeHours} jam`} tone="amber"/><Box label="Tarif Absen per Hari" value={rupiah(p.dailyRate)}/></div><section className="bg-white border border-slate-200 rounded-2xl overflow-hidden"><h2 className="p-4 border-b border-slate-100 font-semibold text-sm">Rincian Perhitungan</h2><div className="p-4"><Line label="Gaji Pokok" value={rupiah(p.base)}/><Line label={`Bonus lembur, ${p.overtimeMinutes} menit disetujui`} value={`+${rupiah(p.bonus)}`} tone="green"/><Line label={`Potongan absen, ${p.absentDays} dari ${p.scheduledDays} hari kerja`} value={`-${rupiah(p.absenceDeduction)}`} tone="red"/><Line label={`Potongan keterlambatan, ${p.lateMinutes} menit`} value={`-${rupiah(p.lateDeduction)}`} tone="red"/><Line label="Estimasi diterima" value={rupiah(p.total)} tone="blue" strong/></div></section></div>}
function Box({label,value,tone}:{label:string;value:string;tone?:string}){const cls=tone==='red'?'bg-red-50 text-red-700':tone==='green'?'bg-emerald-50 text-emerald-700':tone==='amber'?'bg-amber-50 text-amber-700':'bg-white border border-slate-200';return <div className={`rounded-2xl p-4 ${cls}`}><p className="text-xs opacity-70">{label}</p><strong className="block text-xl mt-1">{value}</strong></div>}
function Line({label,value,tone,strong}:{label:string;value:string;tone?:string;strong?:boolean}){return <div className="flex justify-between gap-4 py-3 border-b border-slate-100 last:border-0"><span className={strong?'font-bold':'text-sm text-slate-600'}>{label}</span><span className={`${strong?'font-bold text-lg':'font-semibold text-sm'} ${tone==='red'?'text-red-600':tone==='green'?'text-emerald-700':tone==='blue'?'text-blue-700':''}`}>{value}</span></div>}
