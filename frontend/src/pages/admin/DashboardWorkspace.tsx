import {useEffect,useState} from 'react';
import {api} from '../../lib/apiClient';
import {type Crew,button,panel,money,message} from './adminData';
import {type ManagedEvent,eventHours} from './EventWorkspace';
import {type PayrollAttendance,monthKeys} from './payrollData';
import {MiniChart,Stat,DataTable,EventBadge} from './adminWidgets';
import {wibDate,stamp} from './attendanceData';
export default function DashboardWorkspace(){
 const [crew,setCrew]=useState<Crew[]>([]),[events,setEvents]=useState<ManagedEvent[]>([]),[logs,setLogs]=useState<PayrollAttendance[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
 async function load(){setLoading(true);setError('');try{const [c,e,a]=await Promise.all([api.get<Crew[]>('/crew'),api.get<ManagedEvent[]>('/admin-events'),api.get<{registered:PayrollAttendance[]}>('/admin-attendance')]);setCrew(c);setEvents(e);setLogs(a.registered);}catch(e){setError(message(e));}finally{setLoading(false);}}
 useEffect(()=>{void load();},[]);
 const today=wibDate(),month=today.slice(0,7),months=monthKeys(month),active=crew.filter(c=>c.status==='ACTIVE'),accepted=logs.filter(a=>a.check_in&&a.review_status!=='REJECTED'),current=logs.filter(a=>a.attendance_date===today);
 const days=Array.from({length:7},(_,i)=>{const d=new Date(today+'T12:00:00+07:00');d.setUTCDate(d.getUTCDate()-6+i);return wibDate(d);});
 return <div className="p-4 sm:p-6 space-y-5"><div className="flex justify-between items-center"><div><h2 className="text-lg font-bold text-slate-900">Dashboard</h2><p className="text-xs text-slate-500">Ringkasan {today} • WIB • Data server</p></div><button className={button} disabled={loading} onClick={()=>void load()}>Muat ulang</button></div>{error&&<p role="alert" className="ui-error p-4 rounded-xl">{error}</p>}{loading?<p>Memuat dashboard...</p>:!error&&<><div className="grid sm:grid-cols-2 xl:grid-cols-6 gap-3"><Stat label="Total Crew Aktif" value={active.length} tone="plain"/><Stat label="Crew Event" value={active.filter(c=>c.crew_type==='CREW_EVENT').length} tone="plain"/><Stat label="Crew Store" value={active.filter(c=>c.crew_type==='CREW_STORE').length} tone="plain"/><Stat label="Event Berlangsung" value={events.filter(e=>e.status==='ONGOING').length} tone="plain"/><Stat label="Omzet Bulan Ini" value="Belum tersedia" sub="Modul keuangan belum terhubung" tone="plain"/><Stat label="Payroll Final" value="Belum tersedia" sub="Simulasi tersedia pada menu Payroll" tone="plain"/></div><div className="grid lg:grid-cols-3 gap-4"><MiniChart title="Event per Bulan" subtitle="6 bulan terakhir, berdasarkan tanggal event" points={months.map(m=>({label:m.slice(5),value:events.filter(e=>e.event_date.startsWith(m)&&e.status!=='CANCELLED').length}))}/><MiniChart title="Kehadiran Crew" subtitle="7 hari terakhir, catatan check-in tidak ditolak" color="#059669" points={days.map(d=>({label:d.slice(8),value:accepted.filter(a=>a.attendance_date===d).length}))}/><MiniChart title="Lembur Disetujui" subtitle="6 bulan terakhir, jam penuh dari absensi sah" points={months.map(m=>({label:m.slice(5),value:logs.filter(a=>a.attendance_date.startsWith(m)&&a.check_in&&a.check_out&&['PRESENT','LATE'].includes(a.status)&&['APPROVED','NOT_REQUIRED'].includes(a.review_status)&&a.overtime_status==='APPROVED').reduce((n,a)=>n+Math.floor(a.overtime_minutes/60),0)}))}/></div><div className="grid xl:grid-cols-2 gap-4"><section className="space-y-3"><h3 className="text-sm font-semibold text-slate-900">Event Terbaru</h3><DataTable headers={['Event','Tanggal','PIC','Status']} rows={events.slice(0,5).map(e=>[e.event_name,e.event_date,e.pic?.user.full_name||'Belum ditetapkan',<EventBadge status={e.status}/>])}/></section><section className="space-y-3"><h3 className="text-sm font-semibold text-slate-900">Absensi Hari Ini</h3><DataTable headers={['Crew','Clock In WIB','Status']} rows={current.slice(0,8).map(a=>[crew.find(c=>c.id===a.crew_id)?.user.full_name||'Crew',stamp(a.check_in),a.status])}/></section></div><section className={panel}><h3 className="text-sm font-semibold text-slate-900">Event Sedang Berlangsung</h3><div className="grid md:grid-cols-3 gap-3 mt-4">{events.filter(e=>e.status==='ONGOING').map(e=><div className="bg-blue-50 text-blue-800 rounded-xl p-4" key={e.id}><p className="font-semibold text-sm">{e.event_name}</p><p className="text-xs mt-1">{e.event_locations[0]?.address||'Lokasi belum diisi'}</p><p className="text-xs mt-2">{eventHours(e)} • {e.event_assignments.filter(a=>a.status==='ACTIVE').length} crew</p></div>)}</div>{!events.some(e=>e.status==='ONGOING')&&<p className="text-sm text-slate-500 mt-3">Belum ada event berlangsung.</p>}</section></>}</div>;
}
