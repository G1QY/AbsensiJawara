const test=require('node:test'),assert=require('node:assert/strict');
const {wibDate,combineDateTime,scheduledEnd,overtimeMinutes,lateMinutes}=require('../src/utils/attendanceTime');
test('WIB date does not follow server UTC midnight',()=>{assert.equal(wibDate(new Date('2026-08-30T18:00:00Z')),'2026-08-31');assert.equal(combineDateTime('2026-08-31','09:00:00').toISOString(),'2026-08-31T02:00:00.000Z');});
test('overtime requires a complete hour, not rounded 59m59s',()=>{const end=combineDateTime('2026-08-31','17:00:00');assert.equal(overtimeMinutes(combineDateTime('2026-08-31','17:59:59'),end),0);assert.equal(overtimeMinutes(combineDateTime('2026-08-31','18:00:00'),end),60);assert.equal(overtimeMinutes(combineDateTime('2026-08-31','16:00:00'),end),0);});
test('overnight shift end is next WIB day',()=>{const end=scheduledEnd({schedule_date:'2026-08-31',start_time:'22:00:00',end_time:'06:00:00'});assert.equal(end.toISOString(),'2026-08-31T23:00:00.000Z');assert.equal(overtimeMinutes(combineDateTime('2026-09-01','07:00:00'),end),60);});
test('clock-in 10 or 15 minutes before schedule is allowed and on time',()=>{const start=combineDateTime('2026-09-01','10:00:00');assert.equal(lateMinutes(combineDateTime('2026-09-01','09:45:00'),start),0);assert.equal(lateMinutes(combineDateTime('2026-09-01','09:50:00'),start),0);assert.equal(lateMinutes(combineDateTime('2026-09-01','10:01:00'),start),1);});
